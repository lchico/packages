version: 2
jobs:
  build:
    docker:
      - image: circleci/node
        steps:
          - checkout
          - run:
            name: Greeting
            command: echo Hello, world.
          - run:
            name: Go correct folder
            command: cd test
          - run:
            name: Create package
            command: dpkg --build deb
